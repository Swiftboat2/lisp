;1. Definir la función cuadrado, cubo y luego una función que invoque las anteriores realizando la suma del número, de su cuadrado y del cubo

(
    defun cuad(n)
        (if (= num 0) 0
            (* n n)
            
        )

    defun cub(nu)
        (
            
        )


)